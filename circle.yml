machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  pre:
    - docker build -t koheron/tcp-server .
    - docker run -t -d -p 9200:9200 koheron/tcp-server make DOCKER=True CONFIG=config_local.yaml clean all
